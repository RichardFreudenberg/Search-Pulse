# ─── SearchPulse Pipeline — Python Dependencies ───────────────────────────────

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
pydantic-settings==2.2.1

# Task queue
celery[redis]==5.3.6
redis[asyncio]==5.0.4

# Database
sqlalchemy==2.0.30
alembic==1.13.1
asyncpg==0.29.0          # async PostgreSQL driver (FastAPI)
psycopg2-binary==2.9.9   # sync driver (Celery workers)

# Firebase (write back to CRM)
firebase-admin==6.5.0

# HTTP + web scraping
httpx==0.27.0
playwright==1.44.0
beautifulsoup4==4.12.3
lxml==5.2.2
tenacity==8.3.0

# PDF processing
pdfplumber==0.11.0
PyMuPDF==1.24.3

# Entity resolution / NLP
rapidfuzz==3.9.3
scikit-learn==1.4.2

# Config
pyyaml==6.0.1
python-dotenv==1.0.1

# Utilities
structlog==24.1.0
python-multipart==0.0.9   # FastAPI file uploads

# LLM fallback (optional — only needed for LLMExtractor)
# openai>=1.0.0
# anthropic>=0.25.0

# Testing
pytest==8.2.0
pytest-asyncio==0.23.7
pytest-mock==3.14.0
httpx[asyncio]==0.27.0    # for FastAPI test client
