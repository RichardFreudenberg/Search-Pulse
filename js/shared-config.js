/* ============================================
   Pulse — Shared Deployment Configuration
   ============================================
   API keys entered here are automatically
   configured for every user of this deployment.
   Users will see "Managed by admin" in Settings
   and will not need to enter their own keys.

   Leave a value as '' to let users enter their own.
   ============================================ */

window.PULSE_SHARED_CONFIG = {
  openaiApiKey:       '',   // sk-...          OpenAI (GPT-4o-mini)
  claudeApiKey:       '',   // sk-ant-...      Claude (alternative)
  tavilyApiKey:       '',   // tvly-...        Web research
  firecrawlApiKey:    '',   // fc-...          Web scraping
  rapidApiKey:        '',   //                 RapidAPI (LinkedIn enrichment)
  googlePlacesApiKey: '',   // AIza...         Google Places (Company Scout)
};
