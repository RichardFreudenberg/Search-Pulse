# Claude AI session data — never publish
.claude/
.claudelaunch.json

# Personal / internal docs — keep off the public repo
DSAIL-Submission.html
SearchPulse-Overview.html

# Test files — not needed in production
js/*.test.js

# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Firebase Cloud Functions — secrets and dependencies
functions/.env
functions/.env.local
functions/node_modules/
functions/firebase-debug.log
functions/firebase-debug.*.log

# Pipeline runtime artefacts (contains service account key + scraped data)
pipeline/config/serviceAccountKey.json
pipeline/config/serviceAccountKey.json.json
pipeline/data/
pipeline/__pycache__/
pipeline/connectors/__pycache__/
pipeline/parsing/__pycache__/
pipeline/db/__pycache__/

# Local data backups (may contain PII) — never commit
pipeline/backups/
